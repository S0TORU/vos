---
name: project_vos
type: project
---

# VOS

**What:** Personal Voice Operating System — free local STT/TTS sidecar that boots with recap memory and dispatches to existing coding agents/tools.

**Repo:** `~/vos` (GitHub). Runtime: `~/.vos`.

**Phase:** Scaffold + install + local voice loop. SuperWhisper optional, not required.

**Non-goals:** Rewriting Claude/Codex/Grok configs; always-on daemons by default.
